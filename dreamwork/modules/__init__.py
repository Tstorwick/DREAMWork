"""modules — the branch-ownable pieces of DreamWork.

Each subpackage is one person's slice. They all depend on `core` and never on each other.
See docs/module-contracts.md for the agreement each one keeps.
"""
