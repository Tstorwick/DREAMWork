"""DreamWork — a fundraising CRM for the Extantia portfolio.

See README.md for the big picture and docs/ for the details. The short version:
`core` is the shared foundation, `mcp_server` is the interface, and each piece under
`modules/` is a branch-ownable slice that talks only to `core`.
"""

__version__ = "0.0.1"
